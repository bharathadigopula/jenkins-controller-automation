#==============================================================================
# JENKINS CONFIGURATION AS CODE
#==============================================================================

#==============================================================================
# CONTROLLER CORE CONFIGURATION
#==============================================================================

jenkins:
  systemMessage: Controller configuration is managed as code.
  numExecutors: 0
  mode: NORMAL
  scmCheckoutRetryCount: 2

  #============================================================================
  # NATIVE SECURITY REALM
  #============================================================================

  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: ${JENKINS_ADMIN_ID}
          password: ${JENKINS_ADMIN_PASSWORD}

  #============================================================================
  # AUTHORIZATION STRATEGY
  #============================================================================

  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false

  #============================================================================
  # CONTROLLER ENVIRONMENT
  #============================================================================

  globalNodeProperties:
    - envVars:
        env:
          - key: LANG
            value: C.UTF-8
          - key: LC_ALL
            value: C.UTF-8

  #============================================================================
  # PLATFORM BUILD AGENT
  #============================================================================

  nodes:
    - permanent:
        name: platform-agent
        nodeDescription: Resource-limited Docker build agent on the platform host
        remoteFS: /home/jenkins/agent
        numExecutors: 1
        mode: NORMAL
        labelString: platform docker
        retentionStrategy: always
        launcher:
          inbound:
            workDirSettings:
              disabled: false
              internalDir: remoting
              failIfWorkDirIsMissing: false

#==============================================================================
# CONTROLLER SECURITY HEADERS
#==============================================================================

security:
  contentSecurityPolicy:
    enforce: true

#==============================================================================
# MANAGED CREDENTIALS
#==============================================================================

credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              scope: GLOBAL
              id: github-token
              secret: ${GITHUB_TOKEN}
              description: GitHub automation token
          - usernamePassword:
              scope: GLOBAL
              id: github-scm
              username: bharathadigopula
              password: ${GITHUB_TOKEN}
              description: GitHub source control credential

#==============================================================================
# CONTROLLER LOCATION
#==============================================================================

unclassified:
  globalLibraries:
    libraries:
      - name: jenkins-pipeline-templates
        defaultVersion: v1.3.0
        allowVersionOverride: true
        implicit: false
        includeInChangesets: true
        retriever:
          modernSCM:
            scm:
              gitSource:
                remote: https://github.com/bharathadigopula/jenkins-pipeline-templates.git
  location:
    url: ${JENKINS_URL}
  resourceRoot:
    url: ${JENKINS_RESOURCE_ROOT_URL}

#==============================================================================
# MANAGED PRODUCTION JOBS
#==============================================================================

jobs:
  - script: >
      pipelineJob('configure-production-jenkins') {
        description('Repository-managed Jenkins controller lifecycle')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/bharathadigopula/bharath-oci-host-config.git')
                  credentials('github-scm')
                }
                branch('*/main')
              }
            }
            scriptPath('.jenkins/pipelines/jenkins-controller.groovy')
            lightweight(true)
          }
        }
      }
  - script: >
      pipelineJob('configure-production-monitoring') {
        description('Repository-managed monitoring stack lifecycle')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/bharathadigopula/bharath-oci-host-config.git')
                  credentials('github-scm')
                }
                branch('*/main')
              }
            }
            scriptPath('.jenkins/pipelines/monitoring-stack.groovy')
            lightweight(true)
          }
        }
      }
  - script: >
      pipelineJob('operate-production-oci-infrastructure') {
        description('Repository-managed OCI production Terraform lifecycle')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/bharathadigopula/tf-bharath-oci-infra.git')
                  credentials('github-scm')
                }
                branch('*/main')
              }
            }
            scriptPath('.jenkins/pipelines/production-infrastructure.groovy')
            lightweight(true)
          }
        }
      }
  - script: >
      pipelineJob('operate-production-host-network') {
        description('Repository-managed production secondary IP lifecycle')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/bharathadigopula/bharath-oci-host-config.git')
                  credentials('github-scm')
                }
                branch('*/main')
              }
            }
            scriptPath('.jenkins/pipelines/host-network.groovy')
            lightweight(true)
          }
        }
      }
  - script: >
      pipelineJob('operate-production-ingress-connector') {
        description('Repository-managed production Cloudflare connector lifecycle')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/bharathadigopula/bharath-oci-host-config.git')
                  credentials('github-scm')
                }
                branch('*/main')
              }
            }
            scriptPath('.jenkins/pipelines/ingress-connector.groovy')
            lightweight(true)
          }
        }
      }
  - script: >
      pipelineJob('validate-github-pipeline-templates') {
        description('Validation-only checks for shared GitHub pipeline templates')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/bharathadigopula/github-pipeline-templates.git')
                  credentials('github-scm')
                }
                branch('*/main')
              }
            }
            scriptPath('.jenkins/pipelines/validate.groovy')
            lightweight(true)
          }
        }
      }
  - script: >
      pipelineJob('validate-jenkins-pipeline-templates') {
        description('Validation-only checks for the shared Jenkins library')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/bharathadigopula/jenkins-pipeline-templates.git')
                  credentials('github-scm')
                }
                branch('*/main')
              }
            }
            scriptPath('.jenkins/pipelines/validate.groovy')
            lightweight(true)
          }
        }
      }
  - script: >
      pipelineJob('validate-shared-host-automation') {
        description('Validation-only checks for shared host automation')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/bharathadigopula/shared-host-automation.git')
                  credentials('github-scm')
                }
                branch('*/main')
              }
            }
            scriptPath('.jenkins/pipelines/validate.groovy')
            lightweight(true)
          }
        }
      }
  - script: >
      pipelineJob('validate-terraform-oci-modules') {
        description('Validation-only checks for shared OCI Terraform modules')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/bharathadigopula/terraform-oci-modules.git')
                  credentials('github-scm')
                }
                branch('*/main')
              }
            }
            scriptPath('.jenkins/pipelines/validate.groovy')
            lightweight(true)
          }
        }
      }